<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>com.levynkeneng.display-watcher</string>

	<!-- launchd does not expand $HOME in ProgramArguments, hence the
	     absolute path. Installed by `make install` (copied, not
	     symlinked -- launchd is unreliable with symlinked plists). -->
	<key>ProgramArguments</key>
	<array>
		<string>/Users/levynkeneng/dotfiles/scripts/start-display-watcher</string>
	</array>

	<key>RunAtLoad</key>
	<true/>
	<key>KeepAlive</key>
	<true/>
	<key>ThrottleInterval</key>
	<integer>30</integer>

	<!-- The hook may relaunch sketchybar; don't let launchd reap it
	     when the watcher itself gets restarted -->
	<key>AbandonProcessGroup</key>
	<true/>

	<!-- launchd's default PATH has neither nix nor homebrew; the
	     on-display-change hook needs aerospace and sketchybar -->
	<key>EnvironmentVariables</key>
	<dict>
		<key>PATH</key>
		<string>/run/current-system/sw/bin:/opt/homebrew/bin:/opt/homebrew/sbin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
	</dict>

	<key>StandardErrorPath</key>
	<string>/tmp/display-watcher.err</string>
</dict>
</plist>
